<Window x:Class="Atlantis.Views.ProgresarView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Atlantis.Views"
        xmlns:gif="http://wpfanimatedgif.codeplex.com"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="Exportando" Height="120" Width="300" WindowStyle="None" ResizeMode="NoResize" Background="#2B2B2B" Foreground="White" BorderBrush="#FF333333" BorderThickness="1">
    <Grid>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Exportando Grilla..." HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="18" FontWeight="Bold"/>
            <Image gif:ImageBehavior.RepeatBehavior="3x"
                gif:ImageBehavior.AnimatedSource="/Images/cargando.gif" Margin="0,71,0,10" />
        </Grid>

    </Grid>
</Window>
